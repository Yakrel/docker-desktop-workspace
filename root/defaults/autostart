if [ "$(whoami)" != "abc" ]; then
  sudo lsiown -R $(id -u):$(id -u) \
    /opt/obsidian
fi

gnome-keyring-daemon --start --components=secrets

if [ -n "$KEYBOARD" ]; then
  setxkbmap "$KEYBOARD" &
fi

tint2 &